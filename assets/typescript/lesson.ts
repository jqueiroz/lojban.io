import * as $ from "jquery";

$(document).ready(function() {
    $(".definition-head.transient").attr("title", "No need to memorize this word. It will not be used in further lessons or exercises.");
});
